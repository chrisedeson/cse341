# MongoDB Connection String
# For local development: mongodb://localhost:27017/contacts
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/contacts
MONGODB_URI=your_mongodb_connection_string_here

# Environment (development/production)
NODE_ENV=development

# Port (optional - defaults to 3000)
# PORT=3000

# For production only (automatically set by Render)
# RENDER_EXTERNAL_URL=https://your-app-name.onrender.com

# Optional: Allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS=https://your-frontend-domain.com,https://another-domain.com
